#!/usr/bin/python3
def best_score(a_dictionary):
    max_number = a_dictionary[0]
    if len(a_dictionnary) == 0:
        return None
    max_number = my_list[0]
    for i in range(len(a_dictionnary)):
        if a_dictionnary[i] > max_number:
            max_number = a_dictionnary[i]
    return max_number
